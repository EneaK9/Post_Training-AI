"""Synthetic data: seed cards, briefs, a hidden outcome model, trajectories, comments.

The latent model is the ground truth the fake Meta client draws from. It exists so Loop A
can be tested for the one thing that matters before money is spent: does the loop find the
hidden outliers faster than random selection?
"""
