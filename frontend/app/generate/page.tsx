import { Shell } from "@/components/shell";

export default function GeneratePage() {
  return (
    <Shell>
      <div className="rounded-lg border border-dashed border-line p-8 text-sm text-muted">The Generate screen is built in Phase 5 alongside the episode controller. Use the Data screen and `oai generate` until then.</div>
    </Shell>
  );
}
