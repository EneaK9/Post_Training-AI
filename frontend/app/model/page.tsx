import { Shell } from "@/components/shell";

export default function ModelPage() {
  return (
    <Shell>
      <div className="rounded-lg border border-dashed border-line p-8 text-sm text-muted">The Model screen is built in Phase 5 with Loop A stats and the config editor. Config can be applied through the API until then.</div>
    </Shell>
  );
}
